%% dgdebug -u dice-lite-tests.dg dice-lite.dg unit.dg stdlib.dg

(list of tests [
	#d3
	#d4
	#d6
	#d8
	#d10
	#d12
	#d20
	#d36
	#d72
	#d100
	#d216
    ])

(set up $)	(reset randomness)

%%-----------------------------------------------------------------------------

($ 0 times with max $) %% do nothing

($Dice $N times with max $Max)
	(log) {$Dice $N times with max $Max}
	(roll $Dice $Roll)
	($Roll > 0) ~($Roll > $Max)
	($N minus 1 into $NMinusOne)
	($Dice $NMinusOne times with max $Max)

#d3
(test *)
	(* 1000 times with max 3)

#d4
(test *)
	(* 1000 times with max 4)

#d6
(test *)
	(* 1000 times with max 6)

#d8
(test *)
	(* 1000 times with max 8)

#d10
(test *)
	(* 1000 times with max 10)

#d12
(test *)
	(* 1000 times with max 12)

#d20
(test *)
	(* 1000 times with max 20)

#d36
(test *)
	(* 1000 times with max 36)

#d72
(test *)
	(* 1000 times with max 72)

#d100
(test *)
	(* 1000 times with max 100)

#d216
(test *)
	(* 1000 times with max 216)
